/**
 * @deprecated Use `@/lib/water-port-catalog` — ports are loaded from GET /water/ports.
 * Types re-exported here for backward compatibility.
 */
export type {
  WaterPortOption,
  WaterPortCatalog,
} from '@/lib/water-port-catalog';
