# Copy to .env.local for local dev. On Vercel: add the same keys in Project → Settings → Environment Variables, then redeploy.

# Render FastAPI (optimize, fallback geometry, warmup proxy)
BACKEND_URL=https://logiflow-solution-challenge-2026.onrender.com
NEXT_PUBLIC_API_URL=https://logiflow-solution-challenge-2026.onrender.com

# Supabase — browser reads train_route_geometry + station_coordinates directly (no Render needed for map)
NEXT_PUBLIC_SUPABASE_URL=https://mwvohdvtxwltzkyuboaz.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Optional — live train map overlay
NEXT_PUBLIC_RAILRADAR_API_KEY=
